package com.project.parking.enums;

/**
 * Loại hóa đơn trong hệ thống
 */
public enum InvoiceType {
    MEMBERSHIP, // Phí thành viên (đăng ký gói parking plan)
    PARKING_FEE // Phí đỗ xe lượt (thanh toán khi checkout)
}
